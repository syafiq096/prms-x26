# TypeORM migrations

Reviewed TypeORM migrations are generated into this directory and committed. The API never runs migrations automatically; use the root `migration:*` scripts with the migration-owner role.
