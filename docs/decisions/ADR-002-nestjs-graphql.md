# ADR-002: NestJS GraphQL API

Status: accepted

Use NestJS GraphQL code-first with Apollo Server. Resolvers expose the transport layer, while GraphQL Code Generator produces typed frontend operation artifacts from the generated schema.
