# ADR-008: Terminal Identities and Preserved History

Status: accepted

Mission and resource codes are immutable, deactivated identities are never reactivated, decommissioning is terminal, and domain records are never hard-deleted through the API. New identities are created for replacements so historical usage and audit attribution never change meaning.
