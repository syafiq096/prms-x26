# Backend Design

Use dependency injection, DTOs, services, repository interfaces, guards for authorization, and explicit result/error types. Keep controllers thin and keep membership access checks in a reusable domain policy.
